defaults:
  - data: datasets
  - _self_

# Which datasets to download (set to false to skip)
download:
  coco_val2017: true
  artbench10: true

# Whether to verify checksums after download
verify: true
