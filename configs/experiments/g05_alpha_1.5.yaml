---
# G05 — Phase 3.1: inference-only alpha ablation, lora_scale = 1.5
# NO TRAINING — uses LoRA weights from final Phase 2 config.
job: extension
config:
  name: "g05_alpha_1.5"
  process: []

meta:
  name: "g05_alpha_1.5"
  version: '1.0'
  lora_scale: 1.5
  phase: "3.1"
  base_exp: "dp02_prompt_sks_class"
