---
# G02 — Phase 3.1: inference-only alpha ablation, lora_scale = 0.5
# NO TRAINING — uses LoRA weights from final Phase 2 config.
job: extension
config:
  name: "g02_alpha_0.5"
  process: []

meta:
  name: "g02_alpha_0.5"
  version: '1.0'
  lora_scale: 0.5
  phase: "3.1"
  base_exp: "PHASE2_BEST"
