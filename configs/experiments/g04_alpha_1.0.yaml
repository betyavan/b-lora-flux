---
# G04 — Phase 3.1: inference-only alpha ablation, lora_scale = 1.0 (default)
# NO TRAINING — uses LoRA weights from final Phase 2 config.
job: extension
config:
  name: "g04_alpha_1.0"
  process: []

meta:
  name: "g04_alpha_1.0"
  version: '1.0'
  lora_scale: 1.0
  phase: "3.1"
  base_exp: "PHASE2_BEST"
